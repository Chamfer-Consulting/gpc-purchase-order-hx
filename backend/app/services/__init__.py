"""Service layer — DB queries + pandas transforms, no web framework, no streamlit.

Phase 0.4 / Phase 2 fill this in by moving functions out of dashboard/data.py.
Each module owns one page's data (see docs/REBUILD-TODO.md §0.4)."""
