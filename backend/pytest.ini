[pytest]
# Run from backend/. `pythonpath = .` puts backend/ on sys.path so the tests can
# `from app.main import app` — bare `pytest` (unlike `python -m pytest`) does not
# add the working dir itself.
pythonpath = .
testpaths = tests
addopts = -q
